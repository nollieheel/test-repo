version: 2

jobs:
  build:
    docker:
      - image: circleci/python:2.7.13
    steps:
      - run:
          name: "Nothing"
          command: "true"
  test:
    docker:
      - image: circleci/python:2.7.13
    steps:
      - run:
          name: "Nothing"
          command: "true"
